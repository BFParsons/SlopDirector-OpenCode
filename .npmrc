# Run the project's Unix-style package scripts (inline env vars, rm -rf, &&) on
# native Windows by using Git Bash as the script shell. 8.3 short path avoids the
# space in "Program Files".
script-shell=C:\PROGRA~1\Git\bin\bash.exe
